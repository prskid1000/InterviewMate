# Secrets — NEVER commit API keys
.env

# Local runtime state
overlay_state.json
config.yaml.tmp

# Your personal interview context (uncomment to keep private)
# context/resume.md
# context/job_description.md

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Models cache (faster-whisper etc. if ever used)
*.bin
models/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local config (auto-generated on first run; may hold inline API keys)
config.yaml
